---
- name: Prepare
  hosts: all
  gather_facts: false
  tasks:
    - name: Update apt cache
      apt:
        update_cache: true
        cache_valid_time: 86400
    - name: Install git, vim
      package:
        name: "{{ item }}"
        state: present
      with_items:
        - git
        - vim
